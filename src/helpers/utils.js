import {RFValue} from 'react-native-responsive-fontsize';

export const colors = {
  /**common */
  primaryWhite: '#ffffff',
  primaryBlack: '#000000',
  primaryRed: 'red',
};

export const fontSize = val => RFValue(val, 812);
